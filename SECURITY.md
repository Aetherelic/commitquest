# Security Policy

CommitQuest executes the locally installed `git` binary with argument arrays and does not use a shell for repository parsing. Repository paths and commit text must still be treated as untrusted input.

Please report security issues privately to the repository owner instead of opening a public issue. Include the affected version, reproduction steps, and potential impact.
